import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sample-ios-automate-build',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
